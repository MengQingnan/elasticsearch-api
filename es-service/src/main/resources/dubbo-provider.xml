<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:dubbo="http://code.alibabatech.com/schema/dubbo"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
		http://code.alibabatech.com/schema/dubbo http://code.alibabatech.com/schema/dubbo/dubbo.xsd">

    <!-- dubbo注解扫描包名 -->
    <!--<dubbo:annotation package="cn.zhangxd.platform.system.provider"/>-->

    <!-- 提供方应用信息，用于计算依赖关系 -->
    <dubbo:application name="activity-dubbo-provider" owner="zaodao"  />

    <!--<dubbo:monitor protocol="registry"/>-->

    <!-- 注册中心服务地址 -->
    <dubbo:registry id="zookeeper" protocol="zookeeper" address="${dubbo.registry.address}" />

    <dubbo:protocol name="dubbo" port="40011"/>

    <!-- 声明需要暴露的服务接口 -->
    <dubbo:service interface="com.izaodao.projects.elasticsearch.interfaces.Test"
                   ref="test" version="${dubbo.provider.version}" retries="0" timeout="200000"/>

</beans>
